<style>
  .search-btn{
    background-color: #fe424d;
    color: white;
    border-radius:8rem ;
    padding: 0 1rem 0 1rem;
  }
    .search-btn:hover{
    background-color: #fe424d;
    color: white;
  }

  .search-btn i{
    display: inline;
    font-size: 0.9rem;
    margin-right: 0.5rem;
  }
  .search-inp{
    border-radius: 8rem;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size:0.9rem;
  }
</style>


<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
    <!-- border-bottom (it provide the bottom line for navbar)-->
    <!--  sticky-top(it stick the navbar in main page (see, while scroling the content, there navbar also get scroled so that's why we used {"sticky-top"} )) -->
    <!-- while giveing" background-color: white;", we over come the problem that is, the while will get stick to the main page but content override it, but while giving the bg color white we over come from it -->
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"><i class="fa-solid fa-compass-drafting"></i></a>
    <button 
    class="navbar-toggler" 
    type="button" 
    data-bs-toggle="collapse" 
    data-bs-target="#navbarNavAltMarkup">
    <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <!-- All Listings -->
        <a class="nav-link" href="/listings">Explore</a> 
      </div>

      <!-- Adding the search option (ms-auto (ms -> margin from start))-->
      <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search">
          <input class="form-control me-2 search-inp" type="search" placeholder="Search destinations" />
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>Search
          </button>
        </form>
      </div>
      <!-- ADDING THE LOGOUT AND SIGNUP -->
       <div class="navbar-nav ms-auto">

        <a class="nav-link" href="/listings/new">Airbnb your home</a> <!-- Add new Listing -->
        <!-- if user is not logged in then show login and signup options, if they have login or signup then don't show  -->
        <% if(!currUser){ %>
        <a class="nav-link" href="/signup"><b>Sign up</b></a>
        <a class="nav-link" href="/login"><b>Log in</b></a>
        <%}%>

        <!-- if user is logged in then show logout options -->
        <% if(currUser){ %>
        <a class="nav-link" href="/logout"><b>Log out</b></a>
        <% } %>
        <!-- <a class="nav-link disabled" aria-disabled="true">Disabled</a> -->
      </div>
    </div>
  </div>
</nav>